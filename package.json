{
  "name": "tilesmith-benchmark",
  "version": "0.1.0",
  "private": true,
  "description": "Reproducible quality benchmark and optimization runners for game-ready tiles.",
  "type": "module",
  "scripts": {
    "generate": "tsx scripts/generate-benchmark.ts",
    "ground-truth": "tsx scripts/generate-ground-truth.ts",
    "benchmark": "tsx runners/runner-a-scoring/score.ts",
    "optimize:scoring": "tsx runners/runner-a-scoring/optimize.ts",
    "optimize:pipeline": "tsx runners/runner-b-pipeline/optimize.ts",
    "validate": "tsx scripts/validate-benchmark.ts",
    "test": "tsx --test tests/*.test.ts"
  },
  "dependencies": { "fs-extra": "^11.2.0", "sharp": "^0.33.5" },
  "devDependencies": { "@types/node": "^22.0.0", "tsx": "^4.19.0", "typescript": "^5.6.0" }
}
